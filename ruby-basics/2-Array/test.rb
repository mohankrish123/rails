# Sample array
x = [1,2,3,4]
puts x
puts ""

# Sample array with Array class.
# Can call Array.new with zero, one (the initial size of the Array)
# or two arguments (the initial size and a default object).
a = Array.new(4, "Mohan")
puts a
puts ""


